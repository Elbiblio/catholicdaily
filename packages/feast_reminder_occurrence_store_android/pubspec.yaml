name: feast_reminder_occurrence_store_android
description: Android background-isolate bridge for durable feast reminder claims.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.9.0
  flutter: ">=3.35.0"

dependencies:
  flutter:
    sdk: flutter

flutter:
  plugin:
    platforms:
      android:
        package: com.elbiblio.catholicdaily.occurrencebridge
        pluginClass: FeastReminderOccurrenceStorePlugin
